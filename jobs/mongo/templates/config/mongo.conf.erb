systemLog:
   destination: file
   path: "<%= p("mongo.MONGODB_SYSTEMLOG") %>"
   quiet: false
   logAppend: true

storage:
  dbPath: "<%= p("mongo.MONGODB_STORAGE_DBPATH") %>"
  directoryPerDB: true
  smallFiles: true
  journal:
    enabled: true

net:
  bindIp: <%= p("mongo.MONGODB_BINDIP") %>
  port: <%= p("mongo.MONGODB_PORT") %>
